/**
 * Interactive Cards node half.
 *
 * Provides Cordis lifecycle binding for the interactive cards client plugin,
 * enabling inline sandboxed rendering of HTML, SVG, charts, and interactive components.
 */

export const inject = [];
export const name = "@deepseek-ai/dsh-interactive-cards";

export function apply(ctx) {
  // Client UI discovery is handled via package.json dsh.client declaration.
}
